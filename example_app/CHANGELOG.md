# Example App Changelog

## 0.1.0 (Initial)

- Created example app structure
- Added example event codecs (OrderCreated, TradeExecuted)
- Added benchmark suite:
  - Encode/decode performance
  - Dispatch performance
  - Codec comparison (GridCodec vs JSON, MessagePack, ETF)
- Moved benchmark dependencies from main library
- Set up consolidated code compilation

